apiVersion: v1
kind: Service
metadata:
  name: {{ include "github-issue-sync.fullname" . }}
  labels:
    {{- include "github-issue-sync.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "github-issue-sync.selectorLabels" . | nindent 4 }}
