apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gitspiegel.fullname" . }}-test-connection"
  labels:
    {{- include "gitspiegel.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gitspiegel.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
