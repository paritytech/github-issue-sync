apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "github-issue-sync.fullname" . }}-test-connection"
  labels:
    {{- include "github-issue-sync.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "github-issue-sync.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
